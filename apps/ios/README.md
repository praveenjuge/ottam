# Ottam iOS

The native iOS 18+ SwiftUI application will live here. Its Xcode project and package-local Turbo tasks are introduced in the native-app milestone so CI never carries placeholder device checks.
