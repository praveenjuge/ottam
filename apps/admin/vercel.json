{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "if [ \"$VERCEL_ENV\" = \"production\" ]; then bunx convex deploy --cmd-url-env-var-name NEXT_PUBLIC_CONVEX_URL --cmd 'bun --cwd=../.. run build -- --filter=@ottam/admin'; else bun --cwd=../.. run build -- --filter=@ottam/admin; fi",
  "framework": "nextjs"
}
